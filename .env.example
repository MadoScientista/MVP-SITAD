# JWT Secret - MUST be at least 256 bits for HMAC-SHA
# Generate with: openssl rand -base64 64
JWT_SECRET=JWT_SECRET_REEMPLAZADO

# Database credentials (shared across all MySQL instances)
DB_PASSWORD=DB_PASSWORD_REEMPLAZADO
MYSQL_ROOT_PASSWORD=ROOT_PASSWORD_REEMPLAZADO
